#!/usr/bin/env bash

# Pre-commit hook for CMPM 121 F1 Love2D project
# This hook runs Lua syntax checks before each commit.

echo "🔍 Running Lua pre-commit checks..."

# 1. Make sure luac exists (just like D3 强制要求安装 deno)
if ! command -v luac >/dev/null 2>&1; then
  echo "❌ 'luac' (Lua compiler) is not installed or not in PATH."
  echo "   Please install Lua before committing."
  exit 1
fi

# 2. Get all staged Lua files
lua_files=$(git diff --cached --name-only -- '*.lua')

if [ -z "$lua_files" ]; then
  echo "✅ No Lua files staged. Skipping Lua checks."
  exit 0
fi

echo "📝 Checking Lua syntax..."
failed=0

# 3. Check syntax for each staged Lua file
for file in $lua_files; do
  if [ -f "$file" ]; then
    echo "  • $file"
    if ! luac -p "$file"; then
      echo "❌ Syntax error in $file"
      failed=1
    fi
  fi
done

# 4. If any file failed, block the commit
if [ "$failed" -ne 0 ]; then
  echo ""
  echo "❌ Lua syntax check failed. Fix the errors above before committing."
  exit 1
fi

echo "✅ All Lua pre-commit checks passed!"
exit 0
